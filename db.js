// db.js — placeholder (no-op) so you can add real stats later without breaking imports

export function initDB() {
  // No local database in this clean reset.
  // Keeping a stub so future stats/features can plug in without changing index.js.
  return;
}
